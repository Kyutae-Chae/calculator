upstream myserver {
    server app01:8888;
    server app02:8889;
}

server {
    listen 80;
    server_name localhost;

    location / {
        proxy_pass https://myserver;
        #proxy_pass https://localhost:8888;
    }
}
